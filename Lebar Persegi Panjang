
def luas(panjang, luas):
    if panjang == 0:
        raise ValueError("Panjang tidak boleh nol.")
    return luas / panjang

def keliling(panjang, keliling):
    if panjang == 0:
        raise ValueError("Panjang tidak boleh nol.")
    return (keliling / 2) - panjang
