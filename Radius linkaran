pi = 3.14

def hitung_luas (luas):
  if luas < 0:
        raise ValueError("Luas tidak boleh negatif.")
    return (luas / pi) ** 0.5

def hitung_keliling (keliling):
  if keliling < 0:
        raise ValueError("keliling tidak boleh negatif.")
    return keliling / (pi * 2)
