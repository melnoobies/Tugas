
def luas(lebar, luas):
    if lebar == 0:
        raise ValueError("Lebar tidak boleh nol.")
    return luas / lebar

def keliling(lebar, keliling):
    if lebar == 0:
        raise ValueError("Lebar tidak boleh nol.")
    return (keliling / 2) - lebar
